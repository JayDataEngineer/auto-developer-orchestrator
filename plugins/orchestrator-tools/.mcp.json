{
  "mcpServers": {
    "orchestrator-tools": {
      "command": "uv",
      "args": [
        "run",
        "--no-project",
        "--with",
        "mcp<2",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/server.py"
      ]
    }
  }
}
