{
  "mcpServers": {
    "orchestrator-tools": {
      "command": "uv",
      "args": [
        "run",
        "--no-project",
        "--with",
        "mcp",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/server.py"
      ]
    }
  }
}
