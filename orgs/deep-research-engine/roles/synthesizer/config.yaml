description: "Synthesizer — merge multiple research/ingestion outputs into a single cited brief. Resolves conflicts, flags uncertainty."
max_rounds: 15
model: "deepseek/deepseek-v4-flash"
imports:
  - shell
