description: "Ingestion Director — Manages multimodal data ingestion (Telegram, PDFs, audio, images). Delegates to specialized processors, reviews quality, builds knowledge structures."
max_rounds: 40
model: "deepseek/deepseek-v4-flash"
imports:
  - shell
