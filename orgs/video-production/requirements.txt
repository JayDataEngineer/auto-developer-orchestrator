# Video Production — Python dependencies
# manim for animated visuals
manim>=0.19.0,<0.20.0
# Kokoro TTS for voiceover
kokoro>=0.8.4
# Image processing
Pillow>=10.0.0
# Web requests (source fetching)
httpx>=0.27.0
# JSON/YAML handling
pyyaml>=6.0
