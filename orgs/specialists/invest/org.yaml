agents: [invest-researcher, invest-trader]

# CU-4: foreign MCP tool servers (was policy.yaml ``tool_servers:`` — now a
# permanent contract failure). Bare catalog refs into
# orgs/_shared/tool_servers.yaml.
capabilities:
  - {kind: mcp, ref: equibles}
  - {kind: mcp, ref: web_research}
