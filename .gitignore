# Dependencies
node_modules/
.venv/
venv/

# Build output
build/
dist/
dist-web/
coverage/
*.out
*.exe

# IDE / editor
.DS_Store
*.swp
*.swo
*~

# Environment / secrets
.env*
!.env.example
**/auth.json

# Logs
*.log

# Python bytecode
*.pyc
__pycache__/

# Go binaries
backend/bin/
backend/cli
backend/main
backend/mcpserver
backend/orch
backend/orch-dev
backend/server
backend/tmp/

# Runtime data
# Workspace outputs (artifacts/, memos/, .pux/sessions/) are runtime state —
# never source. The canonical way to retrieve one run's worth of work is
# `pux bundle <thread_id>` → ./<thread_id>.tgz. The bundle output is itself
# ignored (see Archives / backups below) so it isn't committed by accident.
data/
**/.pux/

# Org runtime data (5G+ of model weights, venvs, parsed dumps, memos).
# Org *templates* ship as AGENTS.md + agents/<slug>.md + skills/ + sandbox/ —
# those ARE source. Use profiles/* (not profiles/) so per-org negations can win, then
# exclude everything inside each org except the source files re-negated below.
# General is the sole top-level org; all other orgs live under specialists/.
profiles/*
!profiles/general/
# _shared is not an org (no AGENTS.md/CTO overlay; discover_orgs skips it) but
# holds cross-org SOURCE: agents/ (e.g. researcher) + skills/ + sandbox/ +
# clients/. Must un-ignore the parent or git won't descend into it.
!profiles/_shared/
# Specialists subfolder — nested orgs live here.
!profiles/specialists/
# Ignore everything at depth 2 inside top-level orgs (general, _shared).
profiles/*/*
# Un-ignore org directories inside specialists/ so git can descend into them.
# (They were caught by profiles/*/* above.)
!profiles/specialists/*/
# Re-include source files/dirs at depth 2 for top-level orgs.
!profiles/*/AGENTS.md
!profiles/*/policy.yaml
!profiles/*/org.yaml
!profiles/*/profile.yaml
!profiles/*/tool_servers.yaml
!profiles/*/skills/
!profiles/*/sandbox/
!profiles/*/agents/
!profiles/*/upstream_skills/
# Ignore everything at depth 3 inside specialists/<org>/ (runtime data
# directories, build outputs, etc.).
profiles/specialists/*/*
# Re-include source files/dirs at depth 3 for nested orgs.
!profiles/specialists/*/AGENTS.md
!profiles/specialists/*/policy.yaml
!profiles/specialists/*/org.yaml
!profiles/specialists/*/profile.yaml
!profiles/specialists/*/Dockerfile
!profiles/specialists/*/skills/
!profiles/specialists/*/sandbox/
!profiles/specialists/*/scripts/
!profiles/specialists/*/agents/

# Org-scoped runtime artifacts (article bodies, backtest outputs, parsed
# chat dumps, memos, etc.) — written by the agent during runs, not source.
artifacts/
**/artifacts/
**/workspace/
**/memos/
**/telegram_parsed.json
**/walkthrough_progress.json
**/INVEST-TRACKER.md

# Test artifacts
playwright-report/
test-results/

# Reference repos (cloned, not tracked)
reference/

# Sandbox / projects
projects/

# Browser-test cruft (one-off artifacts from Selenium/xdotool runs)
downloaded_files/
form.html

# Orphan venvs (root .venv/ is covered above; this catches siblings)
.venv-*/

# External research notes (system cards, design docs from outside the repo)
INSIGHTS-*.md

# Subagent default-output pollution — pi-subagents writes to project root
# when an agent's `output:` frontmatter is a bare filename. Operators should
# give explicit paths in task strings; this catches the leaks.
/brief.md
/draft.md
/manifest.md
/options.md
/research.md
/script.md
/test-report.md
/video-productions/

# Internal planning docs
GAPS-*.md

# Archives / backups (never track tarballs)
*.tar.gz
*.tar.bz2
*.tgz
*.zip


# Living notes on browser tool architecture + MCP swap decision
BROWSER-AI-CONTEXT.md

# Hard-won operator context — lessons beaten into Claude across sessions.
# Never commit; this is the local memory of what NOT to get wrong.
CLAUDE-CONTEXT.md

# Exports (not tracked)
export/
=3.4.0
compare_*.png
drafts.json
history.txt
glb_viewer_screenshot.png
final-test
*.bak-check
scripts/*_upload*.png
scripts/compose_state.png
scripts/debug_compose.png
