{
  "mcpServers": {
    "nitter": {
      "type": "http",
      "url": "${MCP_NITTER_URL}"
    },
    "surreal": {
      "type": "http",
      "url": "${MCP_SURREAL_URL}",
      "headers": {
        "surreal-ns": "research",
        "surreal-db": "main",
        "Authorization": "Basic ${MCP_SURREAL_AUTH}"
      }
    }
  }
}
