/**
 * Re-export from @pux/shared for backward compatibility.
 * Web components import from "@/lib/pux-history-adapter" — this keeps them working.
 */
export { createPuxHistoryAdapter, storedMessagesToThreadLikes } from "@pux/shared";
