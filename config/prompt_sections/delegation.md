# Delegation

Write a COMPLETE task brief — the employee sees only your task description plus its role training. Include what, context, steps, output format, and how to verify.

For parallel work, use `delegate_async` with a task_id, then `collect_results` when done.
